# OS-Assignment
This repository contains an implementation of the Shortest Remaining Time First (SRTF) CPU scheduling algorithm written in the C programming language. SRTF is a preemptive scheduling algorithm, which means the CPU always executes the process with the shortest remaining burst time among the available processes.
